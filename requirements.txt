# JARVIS — Python dependencies
# Install with:  pip install -r requirements.txt

rich>=13.0
requests>=2.31
pyttsx3>=2.90
SpeechRecognition>=3.10
wikipedia>=1.4
beautifulsoup4>=4.12
pyjokes>=0.7
psutil>=5.9
pytz>=2024.1
openai>=1.0        # optional — for GPT-4o-mini AI answers
playsound>=1.3

# Optional / advanced features
# pvporcupine        — wake-word detection ("Hey JARVIS")
# opencv-python      — webcam / face detection
# pyaudio            — required by SpeechRecognition on some systems
